name: Test DagsHub MLflow

on:
  workflow_dispatch:

jobs:
  test-dagshub:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install MLflow
        run: pip install mlflow

      - name: Test DagsHub MLflow authentication
        env:
          MLFLOW_TRACKING_USERNAME: ${{ secrets.DAGSHUB_USERNAME }}
          MLFLOW_TRACKING_PASSWORD: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          python - <<'PY'
          import os
          import mlflow

          username = os.getenv("MLFLOW_TRACKING_USERNAME")
          token = os.getenv("MLFLOW_TRACKING_PASSWORD")

          print("Username configured:", bool(username))
          print("Token configured:", bool(token))
          print("Username:", username)
          print("Token length:", len(token) if token else 0)

          if not username or not token:
              raise RuntimeError("DagsHub GitHub secrets are missing")

          mlflow.set_tracking_uri(
              "https://dagshub.com/Kdmodder/GridSight.mlflow"
          )

          client = mlflow.MlflowClient()
          experiments = client.search_experiments()

          print("DagsHub authentication SUCCESS")
          print("Experiments found:", len(experiments))
          PY
